# SPDX-License-Identifier: Apache-2.0
name: "ZXC: Run Examples"

on:
  workflow_call:
    inputs:
      preset:
        description: "CMake preset to build and test (e.g. linux-x64)"
        type: string
        required: false
        default: linux-x64
      build-type:
        description: "CMake build configuration (Debug | Release)"
        type: string
        required: false
        default: Debug
      run-network-examples:
        description: "Also run network examples against solo (slower, needs solo)"
        type: boolean
        required: false
        default: true

  workflow_dispatch:
    inputs:
      preset:
        description: "CMake preset to build and test (e.g. linux-x64)"
        type: string
        required: false
        default: linux-x64
      build-type:
        description: "CMake build configuration (Debug | Release)"
        type: string
        required: false
        default: Debug
      run-network-examples:
        description: "Also run network examples against solo (slower, needs solo)"
        type: boolean
        required: false
        default: true

defaults:
  run:
    shell: bash

permissions:
  contents: read

env:
  PRESET: ${{ inputs.preset || 'linux-x64' }}
  BUILD_TYPE: ${{ inputs.build-type || 'Debug' }}
  BUILD_TYPE_LC: ${{ inputs.build-type == 'Release' && 'release' || 'debug' }}

jobs:
  run-examples:
    name: "Run Examples (${{ inputs.build-type || 'Debug' }})"
    runs-on: hiero-client-sdk-linux-large

    steps:
      # ── Security hardening ─────────────────────────────────────────────────
      - name: Harden Runner
        uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 # v2.19.1
        with:
          egress-policy: audit

      # ── Checkout ───────────────────────────────────────────────────────────
      - name: Checkout Code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: true
          persist-credentials: false

      # ── Build toolchain setup ──────────────────────────────────────────────
      - name: Use Node.js 22
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22

      - name: Install Linux Prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libc6-dbg libgtest-dev build-essential

      - name: Ensure Binary Cache Path Exists
        run: mkdir -p "${{ github.workspace }}/b/vcpkg_cache"

      - name: Install CMake & Ninja
        uses: step-security/get-cmake@082f0e11945209d5c8d8476a791e39d4d041005d # v4.3.1
        with:
          useCloudCache: true

      - name: Setup VCPkg
        uses: step-security/run-vcpkg@48490398616947b0df92dc1b24aab5339a66f9f0 # v11.5.2
        with:
          binaryCachePath: ${{ github.workspace }}/b/vcpkg_cache

      - name: Convert VCPkg to Full Clone
        working-directory: vcpkg
        run: |
          if [ "$(git rev-parse --is-shallow-repository)" = "true" ]; then
            git fetch --unshallow --prune
          fi

      # ── Build (examples only, no tests/TCK to keep it fast) ───────────────
      - name: Configure Project
        env:
          VCPKG_BINARY_SOURCES: clear
        run: |
          echo "::group::CMake Configure"
          cmake --preset "${{ env.PRESET }}-${{ env.BUILD_TYPE_LC }}" \
            -DBUILD_TESTS=OFF \
            -DBUILD_TCK=OFF \
            -DBUILD_TCK_TESTS=OFF \
            -DBUILD_EXAMPLES=ON
          echo "::endgroup::"

      - name: Build Examples
        env:
          VCPKG_BINARY_SOURCES: clear
        run: |
          echo "::group::CMake Build"
          cmake --build --preset "${{ env.PRESET }}-${{ env.BUILD_TYPE_LC }}" -j 6
          echo "::endgroup::"

      # ── Offline examples (no network required) ─────────────────────────────
      - name: Run Offline Examples
        run: |
          echo "::group::CTest – offline examples"
          ctest -C "${{ env.BUILD_TYPE }}" \
                --test-dir "build/${{ env.PRESET }}-${{ env.BUILD_TYPE_LC }}" \
                -L "example-offline" \
                --output-on-failure \
                --no-compress-output \
                -j 4
          echo "::endgroup::"

      # ── Start solo (only when network examples are requested) ──────────────
      - name: Prepare Hiero Solo
        id: solo
        if: ${{ inputs.run-network-examples }}
        uses: hiero-ledger/hiero-solo-action@328bc84c3b00a990a151418144fd682a4eb76ea6 # v0.19.0
        with:
          soloVersion: v0.71.0
          installMirrorNode: true
          mirrorNodeVersion: v0.154.0
          hieroVersion: v0.73.0
          dualMode: true

      # Write .env to the project root so dotenv::init() finds it when CTest
      # runs examples with WORKING_DIRECTORY set to CMAKE_SOURCE_DIR.
      - name: Write .env for Solo Credentials
        if: ${{ inputs.run-network-examples }}
        run: |
          echo "OPERATOR_ID=${{ steps.solo.outputs.OPERATOR_ID }}" >  .env
          echo "OPERATOR_KEY=${{ steps.solo.outputs.OPERATOR_KEY }}" >> .env
          echo "HIERO_NETWORK=localhost"                             >> .env
          echo "MIRROR_NODE=127.0.0.1:5600"                         >> .env
          echo "Wrote .env to project root with solo credentials."

      # Fail fast if HIERO_NETWORK wasn't written — avoids network examples
      # silently falling back to testnet with solo credentials.
      - name: Verify HIERO_NETWORK is set
        if: ${{ inputs.run-network-examples }}
        run: |
          HIERO_NETWORK_VAL="$(grep -E '^HIERO_NETWORK=' .env 2>/dev/null | cut -d'=' -f2 || true)"
          if [[ -z "${HIERO_NETWORK_VAL}" ]]; then
            echo "::error::HIERO_NETWORK is not set in .env — network examples would hit testnet with solo credentials."
            exit 1
          fi
          echo "HIERO_NETWORK=${HIERO_NETWORK_VAL} ✓"

      # ── Network examples ───────────────────────────────────────────────────
      - name: Run Network Examples
        if: ${{ inputs.run-network-examples }}
        # Continue even if some network examples fail so all results are visible;
        # the subsequent reporting step will fail the job if needed.
        continue-on-error: true
        id: network-examples
        run: |
          echo "::group::CTest – network examples"
          ctest -C "${{ env.BUILD_TYPE }}" \
                --test-dir "build/${{ env.PRESET }}-${{ env.BUILD_TYPE_LC }}" \
                -L "example-network" \
                --output-on-failure \
                --no-compress-output \
                -j 2
          echo "::endgroup::"

      # ── Result summary ─────────────────────────────────────────────────────
      - name: Upload CTest Results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: example-ctest-results-${{ env.PRESET }}-${{ env.BUILD_TYPE }}
          path: |
            build/${{ env.PRESET }}-*/Testing/
          if-no-files-found: warn

      # Fail the job explicitly if network examples reported failures so that
      # the continue-on-error above doesn't hide broken examples.
      - name: Fail on Network Example Failures
        if: ${{ inputs.run-network-examples && steps.network-examples.outcome == 'failure' }}
        run: |
          echo "::error::One or more network examples failed. Check the CTest output above."
          exit 1
